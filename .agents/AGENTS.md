# Project Rules: ShopVerse Mini E-Commerce

## Git and Repository Management
- **Do not push changes to the remote repository** (e.g., GitHub via `git push`) unless the user explicitly tells you to do so in the chat. Saving local commits is allowed, but pushes must be user-approved.
